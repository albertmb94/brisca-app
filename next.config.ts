import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Configuración recomendada para Vercel
  // No necesitas output: 'export' porque Vercel soporta SSR nativamente
};

export default nextConfig;
